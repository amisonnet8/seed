# AMIVM上で言語を実装するときのヒント(Seedの実装から)

> 取り込み元: Seed(`github.com/amisonnet8/seed`, commit `46fb3c0`以降)の実装過程で得られた知見。
> 次にAMIVM上で別言語(命令セットを全て使う想定)を実装するAI向けの申し送り。
> Seed自体の言語仕様の話ではなく、**「AMIVM-IRを生成するフロントエンドを書くときに踏む地雷・使える型」の話**に絞っている。
> 初版はamivm commit `cd972aa`時点の知見だったが、その後amivmが単一行`IF boolean1 label`をブロック形`IF`/`ELIF`/`ELSE`/`ENDIF`・`LOOP`/`BREAK`/`CONTINUE`/`ENDLOOP`へ置き換える大きな変更(`253a3fd`、後方互換無し)を行い、Seedもそれに追随した。本書はその移行で得た知見も反映済み(§2が新規追加分)。さらにamivmはメソッド定義・インターフェース・ジェネリクスに相当する`FUNCM`/`INTYPE`/`GETYPE`等を追加(`020e6bc`)したが、こちらはSeedの言語仕様に対応する機能が無く実装変更は不要だった(§6.6が新規追加分。未検証の手がかりとして記載)。さらに名前付き固定長配列型`ARTYPE`(`ac108e9`)と整数リテラルの16進/8進/2進・桁区切り`_`(`22c701f`)が追加されたが、どちらもSeedは採用しなかった(§6.8・§9参照)。

## 0. Seedが実証した命令・していない命令

Seedの言語仕様(1次元固定長配列・スカラー5型・制御構文・関数・簡単なファイルI/O)には、以下の命令だけで足りた。**これらは実際にamivm→go buildまで通して動作確認済み。**

```
VAR GVAR SET ASET AGET
ADD SUB MUL DIV MOD
AND OR NOT
EQ NEQ LT LTE GT GTE
CONCAT
LABEL GOTO
IF ELIF ELSE ENDIF
LOOP BREAK CONTINUE ENDLOOP
FUNC RET ENDFUNC
CALL
SLTYPE SLMAKE
```

`IF`は元々「単一行の条件付きgoto」(`IF boolean1 label`)だったが、amivm commit `253a3fd`でブロック形(Goの`if`/`else if`/`else`に対応)へ全面的に置き換えられ、旧仕様との後方互換は無かった。この移行の詳細と、そこで実際に踏んだ地雷は§2にまとめてある。

**未実証**(Seedの言語仕様に対応する構文が無かったため一度も生成していない):
`BAND BOR BXOR BCLEAR BNOT`(ビット演算)、`SHL SHR`(シフト)、`ADDR PGET PSET`(ポインタ)、`DEFER SPAWN`、`CHTYPE CHMAKE CHSEND CHRECV SEL CASESEND CASERECV DEFAULT ENDSEL`(チャネル・goroutine・select)、`SLICE`(部分切り出し)、`STTYPE FIELD ENDSTTYPE FSET FGET`(構造体)、`MPTYPE MPMAKE MSET MGET MPKEYS`(map)、`FNTYPE CLOS ENDCLOS`(クロージャー・関数型)、`ASSERT`(型アサーション。`any`型自体をSeedが持たないため出番が無い)、`METHVAL FUNCVAL`(メソッド値・関数値取得)、`FUNCM ENDFUNCM`(レシーバー付きメソッド定義)、`INTYPE METHOD ENDINTYPE`(インターフェース)、`GETYPE`(ジェネリクス型実体化)、`ARTYPE`(名前付き固定長配列型。Seedは`SLTYPE`+`SLMAKE`のGoスライスを一貫して使うため出番が無い。§6.8参照)。

次の言語がこれらを使うなら、**§6の「先読み情報」は未検証の推測を含む**ことを念頭に、実装したら必ずamivm+go buildで裏取りすること(§7.1参照)。

## 1. 一番重要な地雷(だった): Goの「gotoが変数宣言を飛び越えてはいけない」ルール

これは元々`LABEL`/`GOTO`と単一行`IF boolean1 label`だけで全ての制御構文を組み立てていた時代の話。**amivmがブロック形`IF`/`LOOP`(§2)を導入した今、素直なif/while/for相当の構文を書くだけならこの節の対策自体は不要になっている**(ブロック命令はGoの本物のネストしたブロックを生成するため、goto自体が発生しない)。それでもこの節を残しているのは、(a) ブロック命令だけでは表現できないジャンプ(ラベル付きbreak/continue相当、複雑な入れ組みジャンプ)がまだ`LABEL`/`GOTO`を必要とすること、(b) Seed自身がブロック形へ移行した後もVAR巻き上げそのものは別の理由(§3の命名スキーム)で維持していること、の2つの理由による。新しい対象言語を素直に設計するなら、まず§2のブロック命令だけで書けないか検討し、書けない場合にだけ以下の対策を適用するとよい。

`LABEL`/`GOTO`で制御構文を組み立てる場合、生成されるGoコードでは**Goのネストしたブロック(`{ }`)に対応しない、1関数内のフラットな命令列**になる。つまり対象言語の`if`/`while`が生成するGo側の構造は、実際には全て同じ関数スコープの中の直列命令+`goto`でしかない。

一方Goには「`goto`は、その時点でスコープに入っていなかった変数のスコープに新たに入るようなジャンプをしてはならない」という規則がある。対象言語で「条件分岐の中でだけ変数を宣言する」ようなコードを書かせると、`break`が生成する前方`GOTO`や`elif`チェーンをスキップする`GOTO`が、スキップした先にある`VAR`宣言を「飛び越える」形になり、**Go側のコンパイルエラーになる**(`goto L jumps over variable declaration`)。これはIRの構文としては何も間違っていないのに、`go build`の段階で初めて発覚する。

**対策**: 対象言語の関数(あるいはクロージャー。§6.4参照)ごとに、**その中で使う`VAR`(ユーザー変数・一時変数の両方)を全て関数の先頭に巻き上げて宣言し、実際の値の代入(`SET`)だけを元の位置に残す。** こうすれば、どんな`GOTO`も「まだ宣言されていない変数」を飛び越えることが物理的に無くなる。

副作用として、「初期化式の無い変数宣言」文も**巻き上げ後は毎回`SET`で明示的にベース値へリセットする**必要がある(ループの2周目以降、巻き上げられた`VAR`は関数開始時に1回しかゼロ初期化されないため、リセットのSETを省略すると前回の値が漏れ残る)。

```
// 悪い例(条件分岐の中でVARを直接宣言。breakのGOTOがこれを飛び越えると go build が失敗する)
IF cond #skip
VAR %x ^int
...
LABEL #skip

// 良い例(関数冒頭で全VARを巻き上げ、実際の初期化はSETで元の位置に残す)
FUNC !f : ^int
    VAR %x ^int      ← 関数の先頭にまとめる
    ...
    IF cond #skip
    SET %x 0          ← 元の位置にはSETだけ残す
    ...
    LABEL #skip
```

## 2. ブロック形`IF`/`LOOP`へ移行して分かったこと(amivm commit `253a3fd`以降)

`IF`が単一行の条件付きgotoからブロック形(`IF`/`ELIF`/`ELSE`/`ENDIF`)へ、また新設の`LOOP`/`BREAK`/`CONTINUE`/`ENDLOOP`が無限`for {}`に対応する形で追加された。素直に使えばGoの本物の`if`/`for`が出るので§1のgoto地雷は消えるが、代わりに次の2つの地雷を実地で踏んだ。

### 2.1 `ELIF`トークンは「既に計算済みの値」しか受け取れない

`ELIF boolean1`のオペランドは、そのIR行に到達した時点で**既に値になっているvariable/literal**でなければならない。対象言語の`elif`条件が単純な変数参照や定数ならそのまま渡せるが、`a + b < c`のような複数命令が要る条件式だと、その計算結果を格納する一時変数への代入をどこかに置く必要がある。しかし「1つ前の節の`}`」と「`} else if`」の間には**文を挟む構文上の余地が無い**(Goの`else if`はそういう構文のため)。

**対策**: `ELIF`トークンをそのまま使わず、**`ELSE`の中に次の`IF`をネストして自前で組み立てる**。

```
// 悪い例が書けない理由の回避: ELIFを使わずELSEでネストする
IF	cond1
	<body1>
ELSE
	<cond2を計算する命令列>   ← ここはELSEの本体なので任意の命令を置ける
	IF	cond2
		<body2>
	ELSE
		<cond3計算…>
		IF	cond3
			<body3>
		ELSE
			<elseBody>
		ENDIF
	ENDIF
ENDIF
```

これはamivm自身が`ELIF`を解析するときにも内部的に構築している構造そのもの(`ELIF`行はamivmのパーサー内で「新たな`IF`として自分自身へ再帰」させ`Else`に据える)なので、生成されるGo ASTは`ELIF`を使った場合と完全に同一になる。しかも各節の条件計算命令が、それを守る`ELSE`の中に自然に収まるため、**短絡評価(前の条件が真ならその後の条件は一切計算しない)も無料で手に入る**。単純な条件(既に変数か定数)しか対象言語が持たないと分かっているなら`ELIF`をそのまま使ってもよいが、迷うなら常にネストで組み立てる方が安全で、コード生成側の分岐も減る。

### 2.2 `continue`が本体の**後**に処理を要求するループ(for-eachのインデックス更新など)は、`CONTINUE`だけでは足りない

`LOOP`の中に「条件チェック→本体」という順で命令を並べる限り、対象言語の`continue`はGoの素の`CONTINUE`(`LOOP`の先頭、つまり条件チェックへ戻る)で正しく動く。しかし、たとえば配列を添字カウンタで舐める`for-each`相当のループのように、**本体の後に「次の要素へ進める」処理(添字++等)がある**設計だと、素の`CONTINUE`はその処理を**飛ばして**しまい、同じ要素で無限ループする。

**対策**: そのループに限り、`continue`は`LABEL`/`GOTO`(ブロック命令へ移行した後も廃止されていない)で、添字++の直前に置いた専用ラベルへジャンプさせる。

```
LOOP
	<条件チェック; NOT ...; IF ... BREAK ENDIF>
	<本体。ここでの break は素のBREAKでよい。continue は GOTO #continue>
	GOTO	#continue   ← 本体の通常フォールスルーにも必ず要る(下記参照)
	LABEL	#continue
	ADD	%idx	%idx	1
ENDLOOP
```

**ここでもう1つ地雷を踏んだ**: 本体に`continue`が1つも無い対象言語コードをコンパイルすると、上記の`LABEL #continue`を参照する`GOTO`が(ユーザーコード由来では)1本も出ないため、`go/types`が`label #continue declared and not used`で拒否する。IRの構文としては正しく見えるのに`go build`で初めて発覚するという、§1と全く同じパターンの地雷である。**対策は、本体の通常フォールスルー経路にも無条件で`GOTO #continue; LABEL #continue`を置くこと**(上記コード例の通り)。こうすれば「ユーザーの`continue`が無い」場合でも、このフォールスルー自身のGOTOがラベルを常に参照するので未使用にならない。

一般化すると: **`break`は常に素の`BREAK`でよい**(最も内側の`LOOP`/`SEL`を抜けるGoの意味論がそのまま対象言語の「最も内側のループを抜ける」に一致するため)。`continue`は「条件再チェックが`LOOP`本体の先頭にある」設計のループなら素の`CONTINUE`で足り、「本体の後に必須の後処理がある」設計のループだけ`LABEL`/`GOTO`を使う必要がある——という判断基準を、ループの種類ごとの設計時点で決めておくとよい。

## 3. スコープとシャドーイング: Go側は完全にフラット

§1の裏返しだが重要なので独立して書く。対象言語が「内側のブロックで外側と同名の変数を再宣言する(シャドーイング)」ことを許すなら、コード生成側は**関数内で一意な内部名を採番して**それぞれ別のGo変数にする必要がある。Go側には対象言語のブロック構造に対応する入れ子スコープが一切存在しない(全て1関数のフラット変数空間)ため、素朴に同じ名前を`%`にそのまま使うと二重宣言エラーになる。**これは§1のgoto地雷とは独立に、VARを巻き上げている限りブロック形`IF`/`LOOP`へ移行した後も変わらず必要**(Seedはブロック形へ移行した後もVAR巻き上げ自体は維持しており、その動機はまさにこの命名スキームが前提にしているため——ブロック形の入れ子スコープをそのままGo側の入れ子スコープとして使い切るように設計し直せば、この節の対策自体が丸ごと不要になる可能性はある。次の言語を最初からブロック形前提で設計するなら検討の価値がある)。

実装パターン: 「宣言のたびに固有のsuffixを振る」スコープスタック(親を辿って名前解決、同一スコープ内の重複だけを禁止)をコード生成側に持たせる。意味検査(sema)側にも同じ形のスコープ構造を独立して持たせてよい(パッケージを跨いだ依存を増やしたくない場合、両方に同じロジックを軽く重複させるのは妥当なトレードオフ)。

## 4. `CALL`は「関数呼び出し」だけではない

- **型キャストと関数呼び出しはIR上区別されない。** Goの`T(v)`という型変換の構文は`ast.CallExpr`と全く同じ形なので、`CALL %n : ?int64 %f`のように**素のGo型名をcallnameとして使う**だけで`n = int64(f)`という型変換になる。対象言語の組み込み型変換のうち、Goの素の型変換で表現できるもの(数値同士など)は、わざわざ独自ランタイム関数を書かなくていい。**ただし`string(intVal)`は罠**: Goの`string(65)`はルーン変換になり`"A"`を返す(10進数の`"65"`にはならない)。数値→文字列の変換だけは`strconv`を使うこと。なお型アサーション(`v.(T)`)はこの`CALL`統合の対象外で、`ASSERT`という専用命令になっている(構文が`ast.CallExpr`ではなく`ast.TypeAssertExpr`になるため)。
- **`CALL`は複数の結果オペランドを取れる**(`CALL multi1 multi2 : callname ...`)。これは単に「複数戻り値を受ける」だけでなく、**Goの`(値, ok)`慣用句(map参照・チャネル受信・`bufio.Scanner`等)を、対象言語の変数が持つ2つの内部オペランド(例: 値+nullフラグ)へ直接書き込む**という使い方ができる。Seedでは`read()`のEOFを`null`として表現するのにこれを使った:
  ```
  CALL	%line	%line_isset	:	?seedrt.Read	%f
  ```
  `seedrt.Read`が`(string, bool)`を返し、2つ目の`bool`(ok)がそのまま対象言語変数の「値がセットされているか」フラグに落ちる。**この技はmap参照(`MGET`)やチャネル受信(`CHRECV`)にもそのまま使えるはず**(どちらもGo側は同じcomma-ok形)。「動的にnullになりうる」値を対象言語がどこかに持つなら、この形が使えないか最初に検討する価値がある。
  - `multi1`が無い(結果を捨てる)場合は、**空文字列オペランドを渡すのではなく本当に省略する**こと(`CALL : callname ...`)。空文字列を渡しても動く場合があるが、それはamivm側のパースが甘いだけで正式な記法ではない。
- **オペランド文字列を組み立ててIRを出力するとき、`%`を含む識別子(`%tmp_2`等)をうっかりGoのフォーマット文字列自体に埋め込まない。** `fmt.Sprintf`等で動的にIR行を作る場合、オペランドは必ず`%s`で置換する引数として渡すこと。フォーマット文字列に直接連結すると、オペランド中の`%`がフォーマット指定子として誤解釈される。

## 5. 参照渡し・値渡しの実現方法

関数パラメータは`$N`という生のオペランドとしてそのまま使える(ローカル変数へコピーする必要が無い)。`SET`の左辺(`single1`カテゴリ)にも`$N`を直接指定できるので、パラメータへの再代入すら特別扱いしなくていい。

- **値渡しにしたい型**(スカラーなど): パラメータを受け取ったら、対象言語側の変数管理の都合(例: null追跡フラグを揃えたい)に応じて、ローカル変数へコピーしてよい。
- **参照渡しにしたい型**(Goの内部表現がスライス・ポインタ・map・チャネル・関数値など、もともと「値渡しでも参照先を共有する」Go型): **パラメータの`$N`をそのままローカル変数の代わりとして使い続け、コピーしない。** Seedの配列(Goスライス)がこの方式で、コピーしないこと自体が「参照渡し」の実現方法そのものになっている。コピーするかしないかは、選んだGo表現がGoの値型かポインタ的な型かで自動的に決まる。

## 6. 未実証の命令カテゴリへの手がかり(§0参照、要実地検証)

以下はSeedでは使わなかったが、amivmの仕様(`amivm/amivm_spec.md`、あるいはこのリポジトリの`CLAUDE.md`に転記済みの内容)から読み取れる生成パターン。**実装したら必ずamivm→go buildの実行結果で裏を取ること**(Seedで見つかった地雷は全て「IR的には正しそうに見えたのにgo buildで初めて壊れているとわかった」パターンだった。§2の2つも同様)。

### 6.1 ポインタ(`ADDR` `PGET` `PSET`)

`single1 = &variable` / `single1 = *variable` / `*single1 = value1`という素直な対応。`ADDR`には省略可能な第3引数`point`もあり、`&variable.field`(構造体フィールドのアドレス)・`&variable[index]`(スライス/配列要素のアドレス)も表現できる。ヒープ確保や、値型を参照渡ししたい場面(構造体を関数間で共有したい等)で使うことになりそう。§5の「参照渡し」を配列以外の型にも広げたいなら、この命令セットが候補になる。

### 6.2 構造体(`STTYPE` `FIELD` `ENDSTTYPE` `FSET` `FGET`)

- `STTYPE`は**トップレベルにしか置けない**(`FUNC`と同じ制約。関数の中でネストした構造体型を定義することはできない)。対象言語が「関数内でしか使わないローカルな構造体型」を許すなら、コード生成側で全ての構造体型定義をトップレベルへ引き上げる必要がある(名前の衝突に注意)。
- フィールド名は`>`プレフィックス。
- メソッド呼び出し(`file.Close()`のような形)は構造体そのものの命令ではなく、`FNTYPE`でレシーバー込みの関数型を宣言→`FGET`でメソッド値を取得→`CALL`という手順(§6.4参照)。`FNTYPE`で宣言した型がGoの実際のメソッド値の型と厳密に一致しない場合は、事前の型宣言を要らない`METHVAL local variable method`(`local := variable.method`)で代替できる(`local`は`VAR`で事前宣言しない、`:=`が使える数少ない例外命令)。Seedは`File`型でどちらの経路も避け、外部パッケージの構造体ポインタ型(`^*seedrt.File`)を直接参照して普通の関数(`?pkg.Func`)で操作する設計にした。**「独自の構造体を定義してメソッド呼び出しをエミュレートする」より「外部パッケージの型をポインタ経由でそのまま使い、操作は全部関数で行う」方が実装コストが低い**、というのがSeedで学んだ教訓。次の言語でも、まずこの逃げ道が使えないか検討する価値がある。既存メソッドの呼び出しではなく、対象言語のユーザーコードが独自の構造体に**メソッドを定義**できる言語機能を持つ場合は`FUNCM`/`ENDFUNCM`(本体内でレシーバーは`$0`)を使うことになる(§6.6参照。Seedは未検証)。

### 6.3 map(`MPTYPE` `MPMAKE` `MSET` `MGET` `MPKEYS`)

`MGET multi1 (multi2) single1 value1`は`single1[value1]`のcomma-ok形をそのまま出せる。§4で書いた「`(値, ok)`をSeed変数の値+nullフラグへ直接流し込む」パターンがそのまま使えるはず(`read()`の`(string, bool)`と全く同じ形)。対象言語に「keyが存在するかどうか」を扱う構文があるなら、これを軸に設計するとよい。`MPKEYS single1 single2`(`single1 = slices.Collect(maps.Keys(single2))`)でmapの全キーを取得できる——AMIVM-IRには`for range`に相当する命令が無いため、mapを走査する唯一の手段はこれになる。

### 6.4 クロージャー・関数型(`FNTYPE` `CLOS` `ENDCLOS`)

- `CLOS`は**ネスト可能**(クロージャーを返すクロージャー=カリー化のような表現用)。ネストの深さ`L`は`FUNC`直下を1として数える。
- クロージャー引数は`$`ではなく**`&`プレフィックス**(`&N`は自分がいる`CLOS`階層のN番目、`&L-N`で階層`L`を明示指定)。関数の中にいるかクロージャーの中にいるか、どの階層のクロージャーの中にいるかで、ローカル変数を指すプレフィックスの使い分けをコード生成側で管理する必要がある。
- **重要**: `CLOS`は実質的にGoの無名関数リテラルを生成するので、**§1の「VAR巻き上げ」を使うならCLOSブロックごとに独立して必要になる**はず(Goのクロージャー本体もそれ自体1つの関数スコープなので、同じ`goto`制約がそのまま当てはまる。ただし§2のブロック形`IF`/`LOOP`だけで書けるなら、この巻き上げ自体が丸ごと不要になり得る点は§1と同じ)。Seedの`funcGen`(1関数分のVAR巻き上げ・ラベル管理・スコープを持つ構造体)を、「FUNCの本体を生成するとき」だけでなく「CLOSの本体を生成するとき」にも使えるよう、**関数本体を表す抽象そのものを再利用可能な形にしておく**とよい。
- `CLOS`の代入先は専用カテゴリを持たず、`single1`(`$N`/`&N`/`&L-N`/`%xxx`/`@xxx`を許容する既存カテゴリ)をそのまま流用する。パラメータやパッケージレベル変数にもクロージャーを代入できる。
- メソッド呼び出しは`FNTYPE`でレシーバー付き関数型を宣言→`FGET`でメソッド値を取得→その値を`CALL`、という手順(§6.2参照)。レシーバーを持たない関数値(パッケージ関数・AMIVM関数・既存の関数値変数)を値として取り出したいだけなら`FUNCVAL local callname`(`local := callname`)の方が単純。

### 6.5 チャネル・goroutine・select(`CHTYPE` `CHMAKE` `CHSEND` `CHRECV` `SEL` `CASESEND` `CASERECV` `DEFAULT` `ENDSEL` `SPAWN` `DEFER`)

- `CHRECV multi1 (multi2) single1`も`MGET`と同じcomma-ok形(`multi2`=falseでチャネルクローズ)。§4のnullパターンがここにも使えそうで、「チャネルから受信できなかった」を対象言語のnull的な値にマッピングできる可能性がある。
- `SEL`(select文)は**ネスト可能**(`IF`/`LOOP`/`CLOS`と同様。§2のブロック形移行と同じタイミングで解禁された)。`CASESEND`/`CASERECV`/`DEFAULT`も`label`を取らないブロック形になっており(旧仕様は`label`への単一行goto)、次の`CASESEND`/`CASERECV`/`DEFAULT`か`ENDSEL`までを自分の本体として持つ。本体には`VAR`宣言を含む任意の命令(`IF`/`LOOP`/`CLOS`/`SEL`のネストを含む)を書ける。`BREAK`はGoの`select`が`for`と同じく`break`の対象になれる挙動をそのまま踏襲し、`LOOP`が無ければ`SEL`自体を抜ける。
- `SPAWN callname value1...` → `go callname(...)`、`DEFER callname value1...` → `defer callname(...)`。対象言語がgoroutineやdeferに相当する構文を持つなら素直に対応する。
- ここは全カテゴリの中で最もGoの意味論に踏み込む部分(select文の公平性、チャネルクローズのタイミング等)なので、対象言語の並行性モデルを固める前に、amivmの`examples/`にある該当サンプルを読んでおくとよい。

### 6.6 メソッド定義・インターフェース・ジェネリクス(`FUNCM` `ENDFUNCM` `INTYPE` `METHOD` `ENDINTYPE` `GETYPE`、および`FUNC`/`STTYPE`/`CALL`/`DEFER`/`SPAWN`のジェネリクス拡張)

Seedにはこれらを使う言語機能(独自メソッド定義・インターフェース・ジェネリクス)が無いため未検証だが、amivm側の命令セットとしては存在する。対象言語がクラス/構造体にメソッドを持たせられる、あるいはジェネリクスを持つ場合の手がかり。

- **メソッド定義**は`FUNC`とは別の専用命令`FUNCM`/`ENDFUNCM`(`STTYPE`で宣言済みの構造体にGoのネイティブなレシーバー付きメソッドを追加する)。本体内でレシーバーは`$0`として参照する(`$0`は`single`/`multi`つまり代入先には使えない——レシーバー自体への再代入を禁止する構造的な制約)。`FUNC`と同じく**トップレベルのみ・ネスト不可**。
- **インターフェース**は`INTYPE typename1 (typename2 constraint1 ...)` → `METHOD method type1 ... : type3 ...`(シグネチャのみ、`func`キーワードなし)を並べる → `ENDINTYPE`。`STTYPE`の`FIELD`に相当する構造。戻り値0個でも`:`は省略できない。
- **ジェネリクス**は`FUNC`/`FUNCM`/`STTYPE`/`INTYPE`に型パラメータ宣言(`typename constraint`の並び)を追加でき、`CALL`/`DEFER`/`SPAWN`は`callname`の直後に明示的型引数を書ける。いずれもコロンの個数で構文が変わる(型パラメータ・型引数を省略すれば従来どおりの構文になる)。`FUNCM`はGoの言語仕様上メソッド自身が独自の型パラメータを宣言できないため、レシーバー側で`STTYPE`側の型パラメータ名をそのまま再掲する(制約は書かない)。
- `STTYPE`/`INTYPE`でジェネリクス宣言した型(`Box<T>`等)は、型引数を当てはめないと`type`カテゴリのトークンとして参照できない。`GETYPE typename1 typename2 type1 ...`(`type typename1 = typename2[type1, ...]`)で具体型に別名を付けることで、以降は普通の型として使えるようになる。
- 実例は`amivm`リポジトリの`examples/19_methval_funcval.ir`・`examples/20_generics_func.ir`・`examples/21_funcm_getype.ir`・`examples/22_intype.ir`を参照。

### 6.7 ビット演算・シフト(`BAND` `BOR` `BXOR` `BCLEAR` `BNOT` `SHL` `SHR`)

これは驚きが少ないはず。`ADD`/`SUB`と全く同じ形の2項(`BNOT`のみ単項)演算命令。Seedで確立した「演算子ごとに命令を振り分け、一時変数へ結果を格納する」パターン(`internal/codegen/expr.go`の`genBinary`)がそのまま流用できる。

### 6.8 名前付き固定長配列型(`ARTYPE`)

`ARTYPE typename1 type1 imm`(`type typename1 [imm]type1`)は、Goのネイティブな固定長配列型`[N]T`をそのまま生成する。`SLTYPE`+`SLMAKE`(Goスライス)とは別物で、**`imm`オペランドは識別子(変数)を一切許さず、リテラルのみ**(`0`,`1234`,`'A'`。5節の`imm`カテゴリ参照)。つまりサイズは構文レベルで完全にコンパイル時定数であることが強制される。

- Seedがこれを採用しなかった理由がそのまま次の言語への判断材料になる: 対象言語の配列(あるいは固定長のリスト的な型)の要素数が、**関数引数やそれ以外の実行時に決まる値であり得るなら`ARTYPE`は使えない**(Seedの`Int[size]`がまさにこのケース)。`SLTYPE`+`SLMAKE`(実行時の`whole`オペランドをそのままサイズに使える)を使うこと。
- 逆に、対象言語が「配列の要素数は必ずソースコード上の定数(あるいはコンパイル時に定数畳み込みできる式)」という制約を持つ言語(Cの固定長配列に近い意味論など)なら、`ARTYPE`はGoスライスよりメモリレイアウトが直接的で、ヒープ確保も発生しない分有利な選択肢になりうる。
- 配列は既存のインライン記法`^[n]type1`(`ARTYPE`を経由せず、型を書ける場所に直接複合形で書く)でも表現できる。`ARTYPE`は名前を付けて複数箇所で使い回したい場合の糖衣構文に過ぎず、必須ではない。
- 実例はamivmリポジトリの`examples/23_artype.ir`を参照。

## 7. 開発プロセスの教訓

### 7.1 機能単位の縦切りステップ + 各ステップでamivm+go buildの実地検証必須

Seedは8ステップ(パイプライン疎通→変数/null→演算子→制御構文→配列→関数→ファイルI/O→CLI/配布)に分けて実装した。**§1・§2のバグは、IRのテキストを目で見て「論理的に正しそう」と判断しただけでは絶対に見つからなかった。実際に`amivm`を呼び、生成されたGoコードを`go build`まで通して初めて発覚した。** amivmがブロック形`IF`/`LOOP`を導入した際も、Seed側の書き換えをユニットテストだけで済ませず実際に新しい`amivm`でビルドし直したところ、§2.2の「for-inのcontinueラベルが未使用」バグが実機で初めて見つかった——移行前と全く同じ教訓が、移行後にもそのまま繰り返された。新しい命令カテゴリを1つ実装するたびに、必ず対応する`.seed`(あるいは新言語の)サンプルを書いて`run`まで確認するサイクルを崩さないこと。

この検証は**amivmリポジトリがPublicになれば、そのままCIに自動化できる**。GitHub Actions上で`go install .../amivm/cmd/amivm@latest`し、対象言語のサンプル一式をビルド→実行するステップを1つ足すだけでよい(Seedの`.github/workflows/test.yml`・`make test-examples`参照)。これにより「amivm本体が更新されるたびに、対象言語側が壊れていないかを毎回手で確認する」という運用コストが、push/PRのたびの自動チェックに置き換わる。amivmが非公開のうちは認証情報が無いとCI上で`go install`できないため、この自動化は後回しにせざるを得ない点に注意。

### 7.2 実際に踏んだバグ(次の言語でも類似バグが起きうる箇所)

1. **§1のgoto/VAR巻き上げ問題**(最重要、上述)
2. **`CALL`の`multi1`省略を「空文字列オペランド」で済ませてしまったバグ**: 動くには動いたが正式な記法ではなかった。「動いた」と「正しい」は別。
3. **意味検査で「値の出どころ」を列挙する際、仕様の実例を見落として過剰に制限したバグ**: Seedの配列は最初「配列変数への代入はリテラル・null・関数呼び出しからのみ」という制限を課したが、**仕様書自身の例(`func Int[] sample(...) { ... return result }`のように、関数内でローカル配列変数をそのまま`return`する)を弾いてしまっていた**。「値がどこから来てよいか」を設計するときは、必ず仕様書中の具体例を全て通しで確認すること。
4. **`fmt.Sprintf`のフォーマット文字列とオペランド文字列の混同**(§4の`%`の罠)。
5. **ブロック形`LOOP`移行時、for-in相当ループの`continue`用ラベルを、本体に`continue`が1つも無い場合でも`LABEL`だけ発行しGOTOを1本も出さなかったバグ**: `go/types`が「declared and not used」で拒否した。フォールスルー経路にも常時1本`GOTO`を置いて解決(§2.2参照)。移行前のgoto連鎖時代には、この「フォールスルーにも明示GOTOを置く」トリック自体は既にあったのに、ブロック形へ書き換える際に一度落としてしまって再発した——**既存コードを別の命令セットへ移植するときは、一見冗長に見える既存の1行にも理由があることを疑うべき**、という教訓。

### 7.3 テスト戦略

- 各レイヤー(sema/codegen)に**Go単体テスト**を持たせ、「このSeedソースをsema.Checkしたらこのエラーになる/ならない」「このIRにはこの命令が含まれる/含まれない」を検証した。IRの厳密な一致ではなく`strings.Contains`ベースの部分一致にしておくと、生成の細部が変わってもテストが壊れにくい。
- ラベル名(`#L1`等)は連番で採番されるため、**特定のラベル番号にベタで依存するテストは書かない**。生成されたIRの中から構造的に(「最初の`LABEL`行」「`LOOP`〜`ENDLOOP`の区間」等)ラベル名や命令の区間を拾ってから比較する。命令セット自体が変わる(§2のような移行)と、この種の構造依存テストは軒並み書き直しが要る——生成形状の詳細に依存するテストは、その分だけ「命令セットの変更に弱い」というコストを引き受けていることを認識しておく。
- 単体テストとは別に、`examples/`配下に**実際に`amivm`→`go build`→実行まで通す**サンプルプログラム群を置き、都度手で実行して出力を目視確認した。単体テストは「生成されたIRの形」までしか保証しないので、最終的にGoとして正しくビルド・動作するかは別途確認が要る。新しい命令セットへ移行したときは、既存のサンプル群を全件回すだけでなく、§2.2のような「本体に`continue`が無いfor-in」のような**境界ケースを突くサンプルを追加**しておくと、同種の未使用ラベルバグを機械的に検出できる。

## 8. うまくいったアーキテクチャ

```
lexer → parser → ast ← sema
                   ↑
                codegen
```

- `ast`パッケージが唯一の共有語彙。`parser`は`lexer`+`ast`に依存、`sema`と`codegen`はどちらも`ast`にのみ依存し、**互いには依存しない**。sema→codegenの情報伝達は、ASTノードに直接アノテーションを書き込む方式にした(下記)。
- **「semaが計算した型情報を、codegenが再計算せずに使う」ためのASTアノテーションフィールド**: 例えば二項演算式のノードに`ResultType`フィールドを持たせ、sema.Checkの型検査中にそこへ確定した型を書き込み、codegenはそれを読むだけにする(スコープを辿った型推論をcodegen側で再度書く必要が無くなる)。多相的な組み込み関数(引数の型によって生成すべき命令が変わるもの)でも同じパターンが使える。
- codegen内部は、最初は1ファイルで始め、肥大化してきたら責務ごとに分割した(`scope.go`=名前解決・内部名採番、本体ファイル=全体オーケストレーション、機能領域ごとのファイル=配列/関数/組み込み関数)。**最初から細かく分割しない。肥大化してから割る。**
- semaは最初「素朴な再帰関数の集まり」で始め、**「プログラム全体で共有する情報(前方参照可能な関数シグネチャ表など)が必要になった時点で」**、小さな構造体(`checker`)を導入してメソッド化した。スコープ(ブロックごとに変わる情報)は構造体のフィールドにはせず、引数として明示的に渡し続けている(スコープは本当に呼び出しごとに変わるが、シグネチャ表はプログラム全体で不変なため)。
- CLIは`flag.NewFlagSet`でサブコマンドごとにフラグ集合を分けるGit風の作り。自前のargvパーサは書かない。

## 9. 細かい実務メモ

- `amivm -i name=path`は**使われなければ自動で消える**ので、使うかどうか分からないパッケージも含めて同じマッピング一式を全IRファイルに使い回してよい(条件分岐で足したり引いたりする必要がない)。
- amivmは**出力先ディレクトリがGoモジュールであること**を要求する(他パッケージ呼び出しの型検査が`golang.org/x/tools/go/packages`に依存するため)。独自ランタイムを呼ぶなら、生成のたびにスクラッチ用の使い捨てGoモジュールディレクトリを用意すること。
- 独自ランタイムパッケージ自体は`go:embed`で自分自身のソースを埋め込み、コンパイラのCLIがビルドのたびにスクラッチモジュール配下へコピーして「同一モジュール内のサブパッケージ」として解決させると、**ネットワークアクセスも`replace`ディレクティブも不要**で完全にセルフコンテインドになる(Seedの`seedrt`パッケージがこの形)。
- Goのラベルは関数スコープ単位なので、ラベル名の連番カウンタは関数(あるいはクロージャー)ごとにリセットしてよい。全関数を跨いだグローバル連番にする必要はない。
- 文字列の「長さ」を持つ組み込み関数を作るなら、Goの素の`len(string)`は**バイト数**であって文字数(rune数)ではないことに注意。多言語テキストを想定するなら`unicode/utf8.RuneCountInString`を使う。
- amivmの`IF`/`ELIF`/`ELSE`/`ENDIF`・`LOOP`/`BREAK`/`CONTINUE`/`ENDLOOP`は、対象言語の`if`/`while`/`for`をコード生成する際に**まず第一候補として検討する**こと。§1の`LABEL`/`GOTO`ベースの巻き上げ方式は、ブロック命令で表現できない場合に限定した「フォールバック」として位置づけるのが、今のamivmの命令セットに一番素直に沿った設計になる。
- 整数リテラルは10進(桁区切り`_`可)に加えて16進(`0x1A`)・8進(`0o17`)・2進(`0b101`)がGoの構文をそのまま踏襲する形で書ける(6節)。対象言語のソース上に同じ記法があるなら、コード生成側で10進へ変換し直さず**そのままIRへ転記してよい**(値としてもGo側の解釈としても等価で、可読性も上がる)。ただし`_`は基数プレフィックスの直後・数字と数字の間にしか置けない(先頭・末尾・連続`_`は不可)ので、対象言語の桁区切り規則がこれと異なる(例: 3桁ごと固定でなく任意位置を許す)場合は素通しせずGo側の規則に合わせて出力すること。Seedの言語仕様(`seed_spec.md`)自体には10進以外のリテラルが無いため、この転記パターンはSeedでは未使用。
