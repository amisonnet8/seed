# ビルド成果物
/seed

# amivmの参照用ローカルクローン(Seedリポジトリの一部ではない。CLAUDE.md参照)
/amivm/

# サンプル(.seed)をコンパイルした際に生成される中間・出力ファイル(.seed → .ir → .go)
examples/**/*.ir
examples/**/*.go
testdata/**/*.ir
testdata/**/*.go

# エディタ・OS由来
.DS_Store

# 一時ファイル
*.tmp
