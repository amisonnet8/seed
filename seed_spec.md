# Seed 言語仕様

## 0. 字句規則

- 改行が文の区切り(セミコロン等は不要)
- `//` 以降、行末までをコメントとして無視する
- 配列は1次元のみ(多次元配列は非対応)

## 1. 型

| 型 | 説明 | ベース値 |
|---|---|---|
| `Int` | 整数型 64bit | `0` |
| `Float` | 浮動小数点型 64bit | `0` |
| `String` | 文字列型 | 空文字 `""` |
| `Bool` | 論理型(`true`/`false`) | `false` |
| `File` | ファイル型 | どのファイルにも紐づいていない状態 |

### null について

宣言直後の変数はまだ値を代入していない状態(`null`)を取りうる。`null`の変数に**アクセスすると、その型のベース値として振る舞う**(`null`であること自体はエラーにならない)。`isnull()`関数で明示的に`null`かどうかを判定できる。

```
Int x        // xはnull。読み取るとベース値0として扱われる
isnull(x)    // true
x = 5
isnull(x)    // false
```

## 2. リテラル

```
null
true
false
1234
-1234
1.234
-1.234
"ABC"
{1, 2, 3}
{1.1, 2.2, 3.3}
{"A", "B", "C"}
```

配列リテラル`{ ... }`は、代入先の型に応じて`Int[]`/`Float[]`/`String[]`のいずれかとして解釈される。

### 文字列リテラルのエスケープシーケンス

文字列リテラル`"..."`内では、Goの[interpreted string literal](https://go.dev/ref/spec#String_literals)と同じエスケープシーケンスを使える(AMIVM-IR・Goへコンパイルされることを踏まえ、両者の仕様にそのまま準拠する)。

| エスケープ | 意味 |
|---|---|
| `\a` | ベル(U+0007) |
| `\b` | バックスペース(U+0008) |
| `\f` | 改ページ(U+000C) |
| `\n` | 改行(U+000A) |
| `\r` | 復帰(U+000D) |
| `\t` | 水平タブ(U+0009) |
| `\v` | 垂直タブ(U+000B) |
| `\\` | バックスラッシュそのもの |
| `\"` | ダブルクォートそのもの |
| `\xHH` | 16進数2桁で指定するバイト値 |
| `\ooo` | 8進数3桁(各桁0〜7、合計で0〜255)で指定するバイト値 |
| `\uXXXX` | 16進数4桁で指定するUnicodeコードポイント |
| `\UXXXXXXXX` | 16進数8桁で指定するUnicodeコードポイント |

上記以外の文字が`\`の直後に続く場合、未終端のまま改行・ファイル末尾に達した場合、`\xHH`/`\uXXXX`/`\UXXXXXXXX`の桁数が不足する場合、`\ooo`の値が255を超える場合は、いずれも字句解析エラーとする。文字列リテラルは常にダブルクォート1行内(`"`で開始し、同じ行の`"`で終端)で完結し、生の改行を含めることはできない(改行が必要な場合は`\n`を使う)。

## 3. 変数・配列の宣言

```
Int x                // 宣言のみ(null)
Int x = 100           // 宣言+初期化
Int[100] x            // 要素数100の固定長配列
Int[2] x = {1, 2}     // 要素数2の配列を宣言し初期化
Int[size] n           // 要素数を変数sizeの値で決める配列(生成時に要素数が確定し、以後は固定長)
```

`Int[size] n`のように要素数を変数で指定した場合も、**生成された時点で要素数は固定される**(可変長配列ではない)。宣言以降にサイズが変わることはない。

### グローバル変数

関数の外(トップレベル)で変数を宣言すると、グローバル変数になる。構文はローカル変数と同じで、**宣言する場所(トップレベルか、関数・ブロックの内側か)によって、グローバルかローカルかが決まる**。

```
Int counter = 0     // トップレベルで宣言 → グローバル変数

func Int increment() {
    counter += 1     // グローバル変数を直接参照・更新できる
    return counter
}
```

### スコープ

- スコープは**ブロック単位**。`{ }`(`if`/`elif`/`else`/`while`/`for`/`func`の本体)ごとに新しいスコープが作られる
- `for x in a { ... }`のループ変数`x`のスコープは、`for`文全体(条件部分含む)。ループを抜けると`x`は参照できなくなる
- **同一スコープ内での同名変数の再宣言はエラー**とする
- **内側のブロックで外側と同名の変数を宣言すること(シャドーイング)は許可する**。内側のブロックの中では、内側で宣言した変数が優先して参照される

```
Int x = 1
if x == 1 {
    Int x = 2         // 外側のxをシャドーイングする(許可される)
    print(string(x))  // 2
}
print(string(x))       // 1(外側のxは変化しない)
```

- グローバル変数のスコープは**プログラム全体**。全ての関数から参照・更新できる
- ローカル変数がグローバル変数と同名の場合、ローカル変数が優先される(シャドーイングの一種)

## 4. 代入

```
i = 100          // スカラーへの代入
i[2] = 200        // 配列要素への代入
i[x] = 300        // 添字に変数を使った配列要素への代入
x = {1, 2, 3}     // 配列全体の再代入
f = 1.1
s = "String"
b = true
```

### 配列への代入における要素数不一致の扱い

配列変数へ、要素数の異なる配列値(リテラル・関数の戻り値のいずれも含む)を代入する場合、次の規則を**共通して**適用する。

- 代入先より要素数が多い場合、超過分は切り捨てる
- 代入先より要素数が少ない場合、不足分は`null`(=ベース値)で埋める

この規則は「配列リテラルの代入」「関数の戻り値として配列を受け取る場合」の両方で同じルールが適用される。

## 5. 演算子

```
+   -   *   /   %      // 算術演算子(2項)
+=  -=  *=  /=  %=      // 複合代入
++  --                  // 後置インクリメント/デクリメント(文としてのみ使用可)
(   )                    // グループ化

==  !=                   // 等価比較
<   <=   >   >=          // 順序比較

&&                        // 論理AND
||                        // 論理OR
!                         // 論理NOT(前置単項)

-                         // 単項マイナス(前置)
```

`++`/`--`/`+=`等の複合代入演算子は、式の一部としてではなく**独立した文としてのみ**使用できる(`x = i++`のような式内での使用はできない)。

### 演算子の優先順位(上から高い順)

| 優先度 | 演算子 | 結合方向 |
|---|---|---|
| 1(最高) | `( )` | - |
| 2 | 単項`!`、単項`-` | 右から左 |
| 3 | `*` `/` `%` | 左から右 |
| 4 | `+` `-` | 左から右 |
| 5 | `<` `<=` `>` `>=` | 左から右 |
| 6 | `==` `!=` | 左から右 |
| 7 | `&&` | 左から右 |
| 8(最低) | `\|\|` | 左から右 |

```
i = (1 + 2) * 3       // 9
b = x == 1 && y > 2 || z != 3
// == の方が && より優先度が高く、&& は || より優先度が高いため
// (x == 1) && (y > 2) || (z != 3) と解釈される
```

### `+`演算子の型ごとの意味

`+`は左右のオペランドの型によって意味が変わる。

- `Int + Int` → 整数の加算
- `Float + Float` → 浮動小数点の加算
- `String + String` → 文字列結合

**異なる型同士の`+`は許可しない**。`Int`と`String`を連結したい場合は、`string()`で明示的に変換してから`+`を使う。

```
s = x + y + z + "."
// x, y, z は全て String 型であることが前提。
// Int や Float を混ぜたい場合は string(i) のように明示変換する
```

## 6. 制御構文

### if / elif / else

```
if x == 100 {
    y = 100
} elif x == 200 {
    z = 200
} else {
    x += 1
}
```

`elif`は0回以上、`else`は0回または1回(いずれも省略可)。

### while

```
while i < x {
    a[i] = 200
    i++
}
```

### for-in

```
a[100] = {1, 2, 3}
for x in a {
    print(string(x))
}
```

配列の要素を先頭から順に走査する。ループ変数(`x`)には要素の値が入る(インデックスではない)。

### break / continue

`while`・`for`のどちらでも使用できる。

- `break`: 最も内側のループを終了する
- `continue`: 最も内側のループの次の周回に進む

## 7. 関数

### 構文

```
func [戻り値の型] 関数名(引数の型 引数名, ...) {
    文
}
```

戻り値の型を省略すると、戻り値なしの関数になる。

```
func xxx() {
    // 戻り値なし、引数なし
}

func String concat(String s1, String s2) {
    return s1 + s2
}

ret = concat("s1", "s2")

func String[] split(String s) {
    // 戻り値: String型の配列(要素数は関数内部で決まる)
}
```

### 配列の引数・戻り値

- **引数として配列を渡す場合、要素数は書かない**(`Type[] 引数名`という形にする)。配列は常に参照渡しになる(関数内での書き換えは呼び出し元にも反映される)
- **戻り値として配列を返す場合も同様に要素数を書かない**(`func Type[] 関数名(...)`という形にする)
- 関数から返された配列を、既存の配列変数へ代入する場合は、4節の「要素数不一致の扱い」がそのまま適用される(超過分切り捨て、不足分は`null`埋め)

```
func Int[] sample(Int[] input) {
    // input を参照渡しで受け取り、何らかの配列を返す
}

Int[5] result
result = sample(someArray)
// sample()の戻り値の要素数が5より多ければ切り捨て、少なければ不足分はnullで埋まる
```

### エントリーポイント

プログラムの実行は`main`という名前の関数から開始する。`main`は以下の固定シグネチャを持つ。

```
func Int main(String[] args) {
    // ここから実行が始まる
}
```

- 引数`args`: コマンドライン引数を`String`の配列として受け取る。`args[0]`はプログラム自身のパス名とする(C言語の慣習に合わせる)
- 戻り値: 終了コードとして扱う。`0`は正常終了、`0`以外は異常終了を表す
- `main`は他の関数から`concat("s1", "s2")`のように直接呼び出すことはできない(エントリーポイント専用)

## 8. ファイル操作

```
File f = open("xxx/xxx.txt", "r")
line = read(f)
write(f, "hello")
close(f)
```

`read(f)`はファイルの終端(EOF)に達すると`null`を返す。`isnull()`でEOF判定ができる。

```
File f = open("xxx/xxx.txt", "r")
while true {
    line = read(f)
    if isnull(line) {
        break
    }
    print(line)
}
close(f)
```

## 9. ビルトイン関数

| 関数 | シグネチャ | 説明 |
|---|---|---|
| `isnull` | `(value: 任意の型) -> Bool` | 引数が`null`かどうかを判定する |
| `int` | `(value: Int \| Float \| String \| Bool) -> Int` | 引数を`Int`に変換する。`Bool`は`true→1`, `false→0`。`String`は数値として解釈できない場合エラー |
| `float` | `(value: Int \| Float \| String) -> Float` | 引数を`Float`に変換する。`String`は数値として解釈できない場合エラー |
| `string` | `(value: Int \| Float \| Bool) -> String` | 引数を`String`に変換する |
| `len` | `(value: String \| 配列) -> Int` | 文字列の場合は文字数、配列の場合は要素数を返す |
| `print` | `(value: String) -> なし` | 文字列を標準出力へ書き、末尾に改行を付ける |
| `open` | `(path: String, mode: String) -> File` | ファイルを開く。`mode`は`"r"`(読み取り)または`"w"`(書き込み) |
| `read` | `(file: File) -> String` | ファイルから1行読み取る。EOFなら`null`を返す |
| `write` | `(file: File, line: String) -> なし` | ファイルへ1行書き込む |
| `close` | `(file: File) -> なし` | ファイルを閉じる |

## 10. キーワード一覧

```
Int Float String Bool File
null true false
if elif else
while for in break continue
func return
isnull int float string len print open read write close
```

## 完成にあたっての設計判断メモ

元の下書きから、以下の点を明確化・修正した。

- 関数宣言は`func [戻り値の型] 名前(...)`という**1つの構文に統一**した(戻り値ありの例で`func`が抜けていた記述は誤記として修正)
- `open`の引数区切りのカンマが抜けていた点を修正(`open("path", "r")`)
- typoを修正(`Stirng`→`String`, `retrun`→`return`, `contine`→`continue`)
- `int[5] = xxxx()`という不完全な例を、`Int[5] result; result = sample(...)`という形に整理し、4節の「要素数不一致の扱い」を関数戻り値の代入にも共通適用されるルールとして一本化した
- 演算子優先順位を明記した(一般的なC系言語の優先順位に準拠)
- `+`演算子が異なる型間では使えないこと(暗黙の型変換をしないこと)を明記した。曖昧だった「文字列結合」の例(`x + y + z + "."`)は、全オペランドが`String`型である前提として補足した
- `print`はString型のみを受け付ける前提とし、数値等を出力する場合は`string()`で明示変換する運用とした
- `read`のEOF時の挙動(`null`を返す)を明記した
- 文字列リテラルのエスケープシーケンスを、Go/AMIVM-IRの`interpreted string literal`仕様に準拠する形で追加した(2節)。従来はエスケープ処理が一切無く、生のバックスラッシュ文字がそのまま文字列値になっていたため、コンパイル先であるGo/AMIVM-IRの文字列リテラル表現との対応が不明瞭だった
